# Security Policy / 安全政策

更新：2026-09-16。中文操作摘要见[安全.md](安全.md)。

## 支持范围

当前开发分支 fix/curl-live-verification 接受安全报告；历史标签用于复现，未承诺安全回补。项目暂无稳定安全支持版本、独立审计或响应 SLA。修复将通过提交和适当公告说明影响范围，不承诺固定修复期限。

## 威胁模型

本软件是本机单用户工具。网页、模型输出和导入请求均不可信。模型只提出计划，确定性代码校验执行。公网目标受策略、DNS/IP 校验、固定连接、同源及 robots 约束，不转发认证信息；cURL 不经 shell 执行。

这些不是生产认证、完整沙箱或法律审查。浏览器运行页面自身脚本；关键词脱敏和 DataPolicy 无法识别全部个人信息、秘密或恶意意图。不要关闭校验来“修好”拒绝任务。参见[技术边界](docs/SECURITY_BOUNDARY.md)。

## 安全部署

- 只绑定 127.0.0.1，不公开端口、反向代理或共享密钥。Host/Origin 检查不是账号认证，本机进程可能读取结果或触发操作。
- 使用专用低额度模型密钥，不提交 .env、Cookie、Authorization、数据库、真实任务产物或含凭证截图。
- 模型 API 是外部接收方，采集允许名单不限制模型供应商地址；核对 HTTPS 域名、处理地点、数据使用政策和费用。
- 确认 POST 仅用于读取；JSON 格式不能证明无副作用。
- 限制页数/条数，按必要期限保存，人工审查后分享。不要将个人信息、国家秘密、重要数据或商业秘密用于演示。
- 定期审查依赖公告；项目没有自动漏洞扫描或零漏洞保证。

## 私下报告漏洞

首选 [GitHub 私密漏洞报告入口](https://github.com/2510192670-heart/Evidence-Aware-Crawler-RAG-Agent/security/advisories/new)。该入口需仓库所有者启用 Private vulnerability reporting；本文件不代表已启用。

若不可用，创建仅含“请求安全私下联系渠道”的普通 Issue，不附载荷、敏感 URL、身份资料或凭证。待维护者提供私密渠道后再发送细节。维护者应在接收实际漏洞前建立私密渠道。

报告包含影响提交、环境、问题类型、预期/实际行为、使用合成数据和自控本机目标的最小复现、影响及缓解建议。不要测试未授权第三方，不窃取数据、不持久化、不造成拒绝服务。

处理流程：确认接收 → 复现分级 → 临时缓解 → 修复回归 → 协商披露。不承诺绝对保密、奖励或法律免责。

## 事件处置

立即停止任务/服务、撤销相关密钥；隔离结果与日志，最小化保留必要证据；核查下载、备份及模型供应商影响。不要公开泄露内容。按法律和合同评估通知、报告及删除义务，必要时寻求专业支持。

数据权利投诉也可先请求私下渠道。维护者不能代操作者处置其本机或供应商保存的数据。
