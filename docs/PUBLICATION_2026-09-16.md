# 2026-09-16 源码整理与发布检查

用户授权整理并推送当前分支，选择标准 MIT。此次不合并 main、不创建 release/tag，不改变历史冻结断言。

## 整理范围

- 提交工作台、数据治理、已有评估 Demo 与测试；工作台功能提交为 `1b814ea`。
- 重写 README，新增安装、安全政策、中文安全操作、隐私数据流、行为准则、贡献指南、MIT、第三方说明、中国法律指南及目标审查模板。
- 增加 PR/缺陷模板；明确 AGENTS 历史里程碑与现行工作台范围的关系，历史报告保留并标注。
- 法规核对使用政府、人大、网信办等正式来源，链接与日期在合规指南。没有宣称律师审查、认证或法律免责。

## 验证

- `.venv/Scripts/python.exe -m pytest tests -q`：1040 passed、1 failed、2 warnings，82.17 秒。唯一失败仍为历史 v0.5.4 源码逐字节冻结检查；未删除、跳过或弱化。
- `npm.cmd --prefix frontend run build`：TypeScript/Vite 通过。
- `.venv/Scripts/python.exe frontend/qa_console.py`：mock 控制台验证通过，零模型调用。
- 独立 Demo 在临时本机 8011 端口验证通过，包括污染拒绝、干净样本、缺字段拒绝、35 对审计及响应原文泄露检查。首次验证受继承代理影响；验证器已使用 trust_env=False，随后通过。临时服务已停止。
- 新文档相对链接检查通过；`git diff --check` 通过。
- 305 个已跟踪/待加入文件及此前 8 个待推送提交差异的敏感文件名、私钥标记和常见供应商 token 格式扫描无命中。此为有限规则检查，不是完整历史密钥扫描或安全审计。
- data/tasks、数据库、环境文件、依赖目录和构建目录不进入提交；没有额外真实模型调用。
- 独立文档复核修正两项表述：总时限失败不同于记录截断，以及固定请求页码约束仅适用于 JSON。其余未发现明确文档发布阻断项。

## 发布边界

目标为 origin 的 fix/curl-live-verification 分支。推送完成与否以 Git 远端提交号为准；本文件不预先声明成功。main 上的 GitHub 社区健康文件和私密漏洞报告设置不会因分支推送自动生效，维护者需按需单独配置。尚未验证全新机器从零安装、完成 SBOM/第三方许可审计或企业合规部署评估。
